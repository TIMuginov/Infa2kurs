import java.io.File;
import java.io.FileNotFoundException;
import java.util.ArrayList;
import java.util.Collections;
import java.util.Comparator;
import java.util.Scanner;

/*public class Collectors {
    public static void zadanie5() throws FileNotFoundException {
        Scanner scanner = new Scanner(new File("sometext.txt"));
        ArrayList<String> line = new ArrayList<>();
        while (scanner.hasNext()) {
            line.add(scanner.nextLine());
        }
        //Лексикографический порядок(по алфавиту)
        Collections.sort(line);
        for (String s : line) {
            System.out.println(s);
        }
        System.out.println();
        //По длине строки
        Collections.sort(line, Comparator.comparingInt(String::length));
        for (String s : line) {
            System.out.println(s);
        }
        System.out.println();
        //По количеству заглавных латинских букв в строке
        Collections.sort(line, new CompareByUpperCase().reversed());
        for (String s : line) {
            System.out.println(s);
        }
    }

    public static void main(String[] args) {

    }

    }
*/
